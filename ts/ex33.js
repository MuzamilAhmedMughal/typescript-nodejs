let arry = [1,2,3,4,5,6,7,8,9];
let i = 0;
while (i < arry.length ) {
    if (arry[i] == 1) {
        console.log(`\n ${arry[i]}st\n`); 
    }
    else if (arry[i] == 2)
    {
    console.log(`\n ${arry[i]}nd\n`);
    }
    else if (arry[i] == 3)
    {
    console.log(`\n ${arry[i]}rd\n`);
    }
    else{
        console.log(`\n ${arry[i]}nd\n`);
    }
    i++

}