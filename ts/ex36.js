
function tshirt() {
  let size = document.getElementById("size").value;
let message = document.getElementById("message").value;
    console.log(` the size of T-shirt is${size}`);
    console.log(` with ${message} printed on it `);
  }