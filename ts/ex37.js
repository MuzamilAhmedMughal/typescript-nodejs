let messagedefult=" i love typescript";
function maketshirt() {
    let size = document.getElementById("size").value;
let message = document.getElementById("message").value;
    console.log(` Defult T-shirt size large with ${messagedefult} printed on it`);
    console.log(`make a ${size} Tsirt with same ${messagedefult} printed on it`);
    console.log(`make a ${size} Tsirt with ${message} printed on it`);

}