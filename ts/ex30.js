let workers=["muzamil","hassan","imran","admin","awaise"];
let i = 0;
while (i < workers.length) {
  if (workers[i] == "admin" ) {
    console.log(`hello ${workers[i]} wouled you like to see status report?`);
  }
  else{
    console.log(`hello ${workers[i]} thankyou for logging again`);
  }
    i++;
}